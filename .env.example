# 复制本文件为 .env.local 后填写。两个变量都留空时，应用会用"内存里的示例数据"运行（第 1 步）。

# 第 2 步：把数据库搬到 Neon 后，填入 Neon 的连接串（Connection string）
# 形如 postgresql://user:password@ep-xxx.ap-southeast-1.aws.neon.tech/dbname?sslmode=require
DATABASE_URL=

# 第 3 步：加"链接带密码"登录。设一个很长的随机字符串。
# 设了之后，只有带 ?key=这串密码 的网址能进；进去后会写入 cookie，之后免输入。
# 留空 = 不设登录（第 1、2 步阶段就留空）。
ACCESS_TOKEN=
